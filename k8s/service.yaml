apiVersion: v1
kind: Service
metadata:
  name: inventario-service
spec:
  type: NodePort
  selector:
    app: inventario
    version: green
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000